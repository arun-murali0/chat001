import { FC } from 'react';

interface ContactsProp {
	profile?: string;
	name?: string;
	messages?: string;
}

export const ContactContainer: FC<ContactsProp> = ({ profile, name, messages }) => {
	return <section>




  </section>;
};
